<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.Book.mapper.WebMapper">
    <update id="setBorrowCost" parameterType="com.Book.entity.BorrowInfo">
        UPDATE borrow_info SET cost = #{cost}
        WHERE code = #{code} AND stu_id = #{stu_id} AND is_return = 0
    </update>
    <update id="setStuCost" parameterType="com.Book.entity.BorrowInfo">
        UPDATE stu_info SET cost = #{cost}
        WHERE stu_id = #{stu_id}
    </update>
</mapper>